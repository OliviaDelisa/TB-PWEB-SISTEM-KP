<main class="main-content">
  <h2>Daftar Pengajuan Dosen Pengganti</h2>

  <div class="search-bar">
    <input type="text" placeholder="Cari">
    <button><i class="fa fa-search"></i></button>
  </div>

  <table class="pengajuan-table">
    <thead>
      <tr>
        <th>NIM</th>
        <th>Nama</th>
        <th>Dosen Sebelumnya</th>
        <th>Status</th>
        <th>Aksi</th>
      </tr>
    </thead>
    <tbody>
      <% daftarPengajuan.forEach(function(p) { %>
        <tr>
          <td><%= p.nim %></td>
          <td><%= p.nama_mahasiswa %></td>
          <td><%= p.dosen_sebelumnya %></td>
          <td><%= p.status %></td>
          <td>
            <a href="/penggantianDosen/detail/<%= p.id %>" class="btn-detail">Lihat Detail</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</main>
